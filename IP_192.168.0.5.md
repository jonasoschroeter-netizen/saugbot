# Raspberry Pi IP-Adresse ✅

## Aktuelle IP-Adresse: **192.168.37.207**

### Verbindung:
- **Hostname:** saugbot.local
- **IP-Adresse:** 192.168.37.207
- **Netzwerk:** 192.168.37.0/24 (neues WLAN)

### Alte IP (vor WLAN-Wechsel): 192.168.0.5

## SSH-Verbindung herstellen:

```bash
ssh pi@saugbot.local
# Oder mit IP:
ssh pi@192.168.37.207
# Password: 123456789
```

## Setup-Script ausführen:

Nach erfolgreicher SSH-Verbindung:

```bash
cd ~
wget https://raw.githubusercontent.com/jonasoschroeter-netizen/saugbot/main/setup_raspi.sh
chmod +x setup_raspi.sh
bash setup_raspi.sh
```

Das Script führt automatisch aus:
- ✅ Git Installation prüfen
- ✅ SSH-Key prüfen/erstellen
- ✅ GitHub Verbindung testen
- ✅ Repository klonen/aktualisieren
- ✅ Python Dependencies installieren
- ✅ .env Datei erstellen
- ✅ GPIO Berechtigungen prüfen

## Nach dem Setup:

```bash
cd ~/saugbot
python3 src/main.py
```

## Web-Interface:

```
http://saugbot.local:5000
# Oder: http://192.168.37.207:5000
```

## Git Workflow:

```bash
cd ~/saugbot
git add .
git commit -m "Beschreibung"
git push origin main
```
